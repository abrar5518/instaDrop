import {notFound} from "next/navigation"; import type {Metadata} from "next"; import Link from "next/link";
const guides={"choose-the-right-courier-vehicle":{title:"How to choose the right courier vehicle",body:["Start with the total weight and the longest, widest and tallest dimensions of the load. Include packaging and pallet dimensions.","Courier cars suit documents, envelopes and small parcels up to 25 kg. Van capacity increases from small and medium vans to large and tail-lift vehicles for pallets or bulky freight.","If the load is unusually shaped or requires mechanical loading, describe this in the quote request so dispatch can confirm a suitable vehicle."]},"prepare-urgent-consignment":{title:"How to prepare an urgent consignment",body:["Use packaging suitable for the item and journey. Secure loose parts and clearly identify fragile or orientation-sensitive goods.","Provide complete collection and delivery addresses, postcodes, access instructions, opening hours and working phone numbers for both ends.","Confirm the item count, weight, dimensions and required collection window before requesting a quote."]},"proof-of-delivery-explained":{title:"Proof of delivery explained",body:["Proof of delivery records that a consignment was handed over. Depending on the booking it may include the recipient name, delivery time, signature or photograph.","Use the tracking reference supplied with the booking on the Track Delivery page. POD becomes available only after dispatch records delivery.","If POD is not yet displayed for a completed job, contact dispatch with the tracking reference."]}} as const;
export function generateStaticParams(){return Object.keys(guides).map(slug=>({slug}))} export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const{slug}=await params;const g=guides[slug as keyof typeof guides];return g?{title:g.title,description:g.body[0],alternates:{canonical:`/blog/${slug}`}}:{}}
export default async function Guide({params}:{params:Promise<{slug:string}>}){const{slug}=await params;const g=guides[slug as keyof typeof guides];if(!g)notFound();return <article className="mx-auto max-w-3xl px-4 py-20"><p className="text-xs font-bold uppercase tracking-widest text-[#0066ff]">InstaDrop guide</p><h1 className="mt-3 text-4xl font-extrabold text-[#0a192f]">{g.title}</h1><div className="mt-8 space-y-5 text-base leading-8 text-slate-600">{g.body.map(x=><p key={x}>{x}</p>)}</div><Link href="/instant-quote#quote-form" className="mt-10 inline-block rounded-full bg-[#c6ff00] px-7 py-3 font-bold text-[#0a192f]">Request a tailored quote</Link></article>}
