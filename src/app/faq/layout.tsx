import type {Metadata} from "next";
export const metadata:Metadata={title:"Courier Questions & Answers",description:"Answers about InstaDrop courier bookings, collection timing, vehicles, tracking and proof of delivery.",alternates:{canonical:"/faq"}};
export default function Layout({children}:{children:React.ReactNode}){return children}
