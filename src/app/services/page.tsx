import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { courierServices } from "@/content/siteContent";
export const metadata: Metadata = { title: "Courier Services | InstaDrop UK", description: "Explore InstaDrop same-day, dedicated vehicle, scheduled, pallet and heavy freight, and wait and return courier services.", alternates:{canonical:"/services"} };
export default function ServicesPage() { return <main className="bg-white"><section className="bg-[#0a192f] px-4 py-20 text-center text-white sm:px-8"><p className="text-xs font-bold uppercase tracking-widest text-[#c6ff00]">Courier services</p><h1 className="mx-auto mt-4 max-w-4xl font-display text-4xl font-extrabold sm:text-6xl">The right delivery method for every urgent movement</h1><p className="mx-auto mt-6 max-w-2xl text-lg text-slate-300">Our five clearly defined courier services make it easy to choose how your consignment should travel.</p></section><section className="px-4 py-20 sm:px-8"><div className="mx-auto grid max-w-7xl gap-7 md:grid-cols-2 lg:grid-cols-3">{courierServices.map((service) => { const Icon = service.icon; return <article key={service.slug} className="flex flex-col rounded-3xl border border-slate-200 p-8 shadow-sm"><div className="mb-5 flex h-12 w-12 items-center justify-center rounded-2xl bg-[#0a192f] text-[#c6ff00]"><Icon className="h-6 w-6" /></div><h2 className="font-display text-2xl font-extrabold text-[#0a192f]">{service.title}</h2><p className="mt-4 flex-1 leading-relaxed text-slate-600">{service.description}</p><Link href={`/${service.slug}`} className="mt-7 inline-flex items-center gap-2 text-sm font-bold text-[#0066ff]">View service <ArrowRight className="h-4 w-4" /></Link></article>; })}</div></section></main>; }
