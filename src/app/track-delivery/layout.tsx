import type {Metadata} from "next";
export const metadata:Metadata={title:"Track Delivery & Proof of Delivery",description:"Check the latest recorded InstaDrop delivery status and access proof of delivery when available.",alternates:{canonical:"/track-delivery"}};
export default function Layout({children}:{children:React.ReactNode}){return children}
