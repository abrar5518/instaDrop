"use client";
/* eslint-disable @next/next/no-img-element -- Logo URL is published dynamically by the admin API. */
import {Zap} from "lucide-react"; import {useContactSettings} from "@/components/Contact/ContactSettings";
export default function BrandLogo({variant}:{variant:"header"|"footer"}){const{branding,business_name}=useContactSettings();const url=variant==="header"?branding.header_logo_url:branding.footer_logo_url;if(url)return <img src={url} alt={`${business_name} ${variant} logo`} className={variant==="header"?"h-14 w-auto max-w-[220px] object-contain":"h-14 w-auto max-w-[240px] object-contain"}/>;return <><div className="flex h-10 w-10 items-center justify-center rounded-xl border border-[#c6ff00]/40 bg-[#0a192f]"><Zap className="h-6 w-6 fill-[#c6ff00] text-[#c6ff00]"/></div><div><span className={`font-display text-2xl font-extrabold ${variant==="footer"?"text-white":"text-[#0a192f]"}`}>Insta<span className="rounded-md bg-[#0a192f] px-1.5 py-0.5 text-[#c6ff00]">Drop</span></span>{variant==="header"&&<span className="mt-0.5 block text-[9px] font-extrabold uppercase tracking-widest text-[#0a192f]">Same-Day Courier</span>}</div></>}
